apiVersion: v1
kind: Service
metadata:
  name: admin-store-service
  namespace: zippyra-prod
spec:
  selector:
    app: admin-store-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
